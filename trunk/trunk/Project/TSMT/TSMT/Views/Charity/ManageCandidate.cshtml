@using TSMT.Models
@model IEnumerable<JoinsIn>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="title_of_page">Manage Candidates of Charity Exam</h2>
<div class="inner-page-content inner-careers">
    <table class="table table-hover table-bordered ">
        <thead>
            <tr>
                <th>STT</th>
                <th>Candidate Name</th>
                <th>Nhóm</th>
                <th>Lodge</th>
                <th>Room</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @{
                int counter = 1;

                foreach (var item in Model)
                {
                <tr>
                    <td>@counter</td>
                    <td>@item.ExaminationsPaper.Candidate.Account.Profile.Lastname @item.ExaminationsPaper.Candidate.Account.Profile.Firstname</td>
                    <td>
                        @if (item.ExaminationsPaper.GroupID != null)
                        {
                            @item.ExaminationsPaper.GroupID
                        } 
                            else
                        {
                            <span>Không thuộc nhóm nào</span>
                        }
                    </td>
                    <td>
                        @if (item.RoomID != null)
                        {
                            @item.Room.Lodge.LodgeID
                        }
                        else
                        {
                            <span>Chưa được sắp xếp</span>
                        }
                    </td>
                    <td>
                        @if (item.RoomID != null)
                        {
                            @item.Room.RoomID
                        }
                        else
                        {
                            <span>Chưa được sắp xếp</span>
                        }
                    </td>
                    <td>
                        @Html.ActionLink("Details", "DetailsCandidate", new { joininId = @item.JoinInID })
                        @Html.ActionLink("Remove", "RemoveCandidate", new { joininId = @item.JoinInID }, new { onclick="return showPopup('Are you sure to delete it?', this.href);"})
                    </td>
                </tr>
                        ++counter;
                }
            }
        </tbody>
    </table>
</div>
@Html.ActionLink("Back to Manage CE", "DetailsCharityExam", new { ceId = @ViewData["ceId"] }, new { @class = "btn btn-info" })
@*<script>
    function DeleteConfirm() {
        if (confirm("Bạn chắc chắn muốn xóa ?"))
            return true;
        else
            return false;
    }
</script>*@