@using TSMT.Models
@model IEnumerable<ChairitiesExam>
@{
    ViewBag.Title = "AddCharityExam";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
}

<h2 class="title_of_page">Add Lodge</h2>
@using (Html.BeginForm("AddLodge", "Sponsor", FormMethod.Post))
{
    <table>
      @*  <tr>
            <td>Kỳ thi + Tổ chức</td>
            <td>
                <select name="ceId">
                    @foreach (var item in Model)
                    {
                        <option value="@item.CharityExamID">@item.Examination.Name - @item.CharityExamName</option>
                    }
                </select>
            </td>
        </tr>*@
        <tr>
            <td>Địa chỉ</td>
            <td>
                <input type="text" name="Address" /></td>
        </tr>
        <tr>
            <td colspan="2">
                <input type="submit" value="Create" class="btn btn-info"  />
                <input type="reset" value="Reset" class="btn btn-info" />
            </td>
        </tr>
    </table>
}
